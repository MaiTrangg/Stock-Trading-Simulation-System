spring:
  application:
    name: stock-trading-system
  datasource:
    url: jdbc:postgresql://localhost:5432/trading_system_db
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: kimdohoon724@gmail.com
    password: flvg mtnq lhum ehjk #flvg mtnq lhum ehjk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  mcp:
    url: http://localhost:3333/call

  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:
            scope:
              - email
              - profile
        provider:
          google:
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
  logging:
    level:
      org.springframework.security: DEBUG